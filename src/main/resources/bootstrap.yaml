spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/newtonmn/config-server-repo.git            application:    name: Config-Server-proj21
          
server:  port: 8101

management:
  endpoints:
    web:
      exposure:
        include: "*"
  